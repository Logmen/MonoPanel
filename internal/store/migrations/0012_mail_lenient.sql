-- Домен-приёмник: почта принимается без строгих проверок HELO и отправителя.
-- Нужен там, где входящее письмо — это предмет анализа, а не корреспонденция:
-- диагностический приёмник должен увидеть и криво настроенного отправителя.
ALTER TABLE mail_domains ADD COLUMN lenient INTEGER NOT NULL DEFAULT 0;
