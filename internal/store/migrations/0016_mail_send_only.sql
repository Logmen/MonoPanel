-- Домен «только отправка»: почту домена принимает другой сервер (MX у почтового
-- провайдера), а этот только подписывает DKIM и отправляет письма сайтов.
-- В картах приёма postfix его нет, поэтому письмо сайта на адрес этого же
-- домена уходит на настоящий MX, а не в несуществующий локальный ящик.
ALTER TABLE mail_domains ADD COLUMN send_only INTEGER NOT NULL DEFAULT 0;
