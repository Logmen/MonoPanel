# Fail2Ban filter for MonoPanel login failures (journald, monopanel-api.service)
[Definition]
failregex = ^.*msg="login denied".*ip=<HOST>.*$
ignoreregex =
journalmatch = _SYSTEMD_UNIT=monopanel-api.service
