# Generated by MonoPanel — memcached, local only. Custom settings: mp stack memcached --memory-mb … --max-conn …
PORT="11211"
USER="{{ .User }}"
MAXCONN="{{ .MaxConn }}"
CACHESIZE="{{ .MemoryMB }}"
OPTIONS="-l {{ .Listen }}"
