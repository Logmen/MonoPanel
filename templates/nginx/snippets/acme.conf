# ACME HTTP-01 challenges are served from a shared webroot on every :80 server.
location ^~ /.well-known/acme-challenge/ {
    root /var/lib/monopanel/acme/webroot;
    default_type text/plain;
    allow all;
}
